<h1>All Pets for <%= @owner.email %></h1>
<hr>
<% @owner.pets.each do |pet| %>
  <p>
    <strong><%= link_to "#{pet.name}", pet_path(pet)  %></strong>
  </p>
<% end %>
<strong><%= link_to "add a new pet", new_owner_pet_path(@owner)  %></strong>
